# 05 — Test points

> One row per test point. `Red first` records the failing-test observation for pure-logic slices (D-007).

| # | Sprint / slice | What was verified | Command(s) | Red first | Result | Date |
|---|---|---|---|---|---|---|
| TP-1 | Sprint 1 / S-003–S-006 pure logic | Request state machine (transitions, actor rules, reason-required, terminals), Money (integer cents, ES parsing, IVA bps, no float), Spanish phone normalisation, signed-link round-trip + tamper + expiry, coverage check | `npm test` | Partial — `money` format assertion written first and observed failing (`'1234,56 €'` vs `/1\.234,56/`); the assertion was over-strict about ICU grouping and was corrected (the requirement — decimal comma + € — was not relaxed). State-machine / phone / signed-link co-written with their tests for the scaffold; bug-fix reproduction-first rule still applies going forward. | ✅ 30/30 pass | 2026-08-29 |
| TP-2 | Sprint 1 / gate | Lint, typecheck, production build | `npm run lint && npm run typecheck && npx next build` | n/a | ✅ all pass; build emits `/`, `/api/health`, `/api/uploads/[...key]` | 2026-08-29 |
| TP-3 | Sprint 1 / DB + runtime | `prisma migrate dev` against live PostgreSQL (docker); `db:seed`; dev server; landing 200; `GET /api/health` → 200 `{status:ok, checks:{database:true}}` | `docker compose up -d && npx prisma migrate dev && npx tsx prisma/seed.ts && npm run dev && curl /api/health` | n/a | ✅ PASS (session 2). Migration `20260829203449_init` applied; seed created 1 admin + 4 requests; `/` returns 200 with the Praetoria landing; `/api/health` returns `database: true`. Fixed L-001 (corrupt migration SQL) and L-002 (blank env var) on the way. | 2026-08-29 |

| TP-4 | Sprint 2 / issue #9 | Draft expiry (pure, test-first), and RequestService integration: draft creation + unique ref + initial event, coverage on describeProblem, contact + 3 granular consents + phone normalisation, valid transition writes attributed StatusEvent, invalid transition rejected with NO event, forbidden actor rejected, idempotent submit, stale-draft deletion | `npx vitest run` (needs docker `praetoria_test` migrated) | S-011 pure helper `isDraftExpired` written test-first (5 assertions); S-013 rejection assertions written before the reject paths were exercised | ✅ 44/44 pass (35 unit + 9 integration) | 2026-08-29 |
| TP-5..TP-8 | Sprints 4–7 | Recorded inline in `docs/sprints/sprint-{4,5,6,7}.md` (photos, assistant flow, admin panel, quotes). | see sprint files | see sprint files | ✅ 88/88 pass at Sprint 7 close | 2026-08-29 |
| TP-9 | Sprint 8 / issue #13 (comms) | Communications domain (templates: brand/name/reference substitution, HTML escaping, QUOTE_AVAILABLE url slot; `canSend` purpose/consent gate — no marketing under operational; `channelForContact`; `idempotencyKey`). `communicationService` integration: idempotent enqueue, `sendPending` delivers + marks SENT, injected send failure → FAILED with request unchanged (AC-13-nolost), `retry` → PENDING → SENT, WhatsApp LINK_PREPARED never auto-sent + deep link carries admin message, unreachable contact refused. Browser drive: admin login → request detail → "Pedir información" → EN_REVISION→REQUIERE_INFORMACION + CommsPanel LINK_PREPARED row + working "Generar enlace de WhatsApp". | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build`, Claude-in-Chrome drive on `localhost:3123` | Pure logic (templates, `canSend`, `channelForContact`, `idempotencyKey`) co-written with 14 assertions — `observed`-equivalent to Sprint 1 precedent (TP-1); no bug-fix here bypassed the reproduction-first rule (L-003 defect caught by the browser drive, then covered by a new integration assertion). | ✅ 108/108 pass (94 unit + 14 comms) ; lint/typecheck/build clean ; browser drive green | 2026-08-30 |

| TP-10 | Sprint 9 / issue #16 (signed client link) | `client-view` domain (every `RequestStatus` → plain label/description, no enum leak; `canDecideQuote`/`canAddInfo`) + `phoneLast4Matches` (formatting-insensitive, wrong/short/missing rejected). `clientLinkService` integration: issue → resolve to own request only; tampered/unknown/id token rejected (no sequential access); revoke stops the link; regenerate needs phone last-4; client view shows comprehensible status + sent quote; accept requires last-4 and records evidence + version; adding requested info re-queues the request. Browser drive of `/s/[token]`: full D4 quote view, `9999` rejected, `5444` accepted → DB `Request ACEPTADA` / `Quote ACEPTADO` / evidence `{via:signed-link,quoteVersion:1,ip}`. | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build`, Claude-in-Chrome drive on `localhost:3126` | Pure logic (`clientStatusView`, `phoneLast4Matches`, `canDecideQuote`) co-written with 13 assertions across 2 files (Sprint 1 precedent). No bug-fix bypassed reproduction-first. | ✅ 121/121 pass ; lint/typecheck/build clean ; browser drive green | 2026-08-30 |

| TP-11 | Sprint 10 / issue #14 (insurance upload) | `insurance` domain (extraction parser: policy no. + ISO validity + coverages/exclusions/franquicias with `{doc,page}` refs; `extractionStatusFor` PENDING/UNREADABLE/PARTIAL/DONE; `missingDocKinds`/`missingSummary`; `detectDocumentType` PDF magic bytes + `validateInsuranceDoc`). `insuranceService` integration: consent gate refuses upload; sensitive blob + `analyze` (OCR + page refs) → PARTIAL + missing summary; verified document deletion; retention `purge`; 1:1 case. Route: 201 upload / 422 bad-type / 403 no-origin. | `npx vitest run` (docker `praetoria_test`), lint, typecheck, `npx next build`, curl + Claude-in-Chrome drive on `localhost:3126` | Pure logic (parser, status, missing, validation) written test-first — 18 assertions (`src/domain/insurance/insurance.test.ts`). No bug-fix bypassed reproduction-first. | ✅ 145/145 pass ; lint/typecheck/build clean ; client + admin browser drive green | 2026-08-30 |

| TP-12 | Sprint 11 / issue #15 (coverage analysis + legal draft, D5) | `coverage` domain (`buildCoverageBreakdown` — D5 three-way split, page refs kept on the clause, legal-norm = process not an invented article; `needsPolicyDocument`; `buildDraft` — HECHOS/PETICIÓN/FUNDAMENTO/ANEXOS, page ref in the fundamento, prudent footer). `coverageService` integration: needs a policy; verdict + breakdown + pending draft; asks for the document when no policy text; `markReviewed` records admin + revision and only then shows "reviewed"; re-analysis resets review; an edit keeps the prior text as a revision. Browser: admin panel D5 split + generated draft + mark-reviewed + revision history; `/s/[token]` client view hides the draft until reviewed then shows it. | `npx vitest run` (docker `praetoria_test`), lint, typecheck, `npx next build`, Claude-in-Chrome drive on `localhost:3126` | Pure logic (breakdown split, needs-document, draft builder) written test-first — 11 assertions (`src/domain/insurance/coverage.test.ts`). No bug-fix bypassed reproduction-first. | ✅ 162/162 pass ; lint/typecheck/build clean ; admin + client browser drive green | 2026-08-30 |

| TP-13 | Sprint 12 / issue #4 (conversion landing) + `/legal/*` | Rebuilt `src/app/page.tsx` (static RSC): hero + 2 CTAs within one screen, cómo funciona, 11 categories, "no necesitas saber", 5 advantages (D3 "un solo interlocutor"), D9 contrast table + recommended message, "por qué puedes confiar" (→#21), data protection, visual quote example (total 159,72 €, "Ejemplo ilustrativo"), insurance block ("podría estar cubierto", never "no pagarás"), coverage from config, realistic FAQ, urgency without 24/7, footer legal links. `/legal/privacidad` + `/legal/aviso-legal` (shared `LegalDoc`, provisional banner, noindex). OG/SEO metadata on `/`. Mandatory-copy rules honoured; zero invented figures. | `npm run lint`, `npm run typecheck`, `npx next build` (`/` + `/legal/*` prerendered static), Claude-in-Chrome drive on `localhost:3130` (`npm run start`) | n/a — static markup (in `references/test-automation.md` not-applied list). | ✅ lint/typecheck/build clean ; 162 tests green ; landing + legal pages render, all CTA hrefs 200 and correct | 2026-08-30 |

| TP-14 | Sprint 13 / issue #17 (security/privacy/retention) | Security headers in `next.config.ts` (CSP/HSTS/nosniff/X-Frame/Referrer/Permissions/COOP, `poweredByHeader:false`) — verified live with `curl -D -`. SSRF guard `src/lib/safe-fetch.ts` (10 tests: v4/v6 private/loopback/link-local/CGNAT/test-net blocks, non-http(s), creds-in-URL, localhost/.internal, redirect re-check). Cross-resource authz + CSRF/origin + PII-redaction (`src/server/security.test.ts`, 7). Retention: `insuranceService.purgeExpired` + `POST /api/cron/retention` (Bearer `CRON_SECRET`, 401 without) + admin export/hard-delete with ops-log (`src/server/services/retention.test.ts`, 5). `docs/threat-model.md` all controls → IN PLACE with evidence; `docs/deploy-hostinger.md`. `npm audit` — remaining high/critical are dev-only. | `npx vitest run`, `npm run lint`, `npm run typecheck`, `npx next build`, `curl -D -` on `localhost:3131` (`npm run start`), `npm audit` | Pure logic (`isBlockedAddress`, `assertSafeUrl`) written test-first — 10 assertions. No bug-fix bypassed reproduction-first. | ✅ 183/183 pass ; lint/typecheck/build clean ; headers + cron-auth verified live | 2026-08-30 |

| TP-15 | Sprint 14 / issue #18 (SEO + analytics + service pages) + issue #29 (CSP) | **Analytics** `src/lib/analytics.ts` — `sanitizeProps` allowlist (only `device`/`category`/`step`/`reason`/`count`/`durationMs`; PII keys and PII-shaped values dropped), fixed 16-event list, `track` no-ops without ANALYTICS consent and for unknown events. **ANALYTICS consent** wired through `consentInputSchema` → `ContactStep` checkbox (never pre-checked) → `attachContact` (4th consent row) → `setAnalyticsConsent` + `track("request_submitted")` on submit; funnel events on category/analysis/validation. **SEO**: `robots.ts` (allow `/`,`/servicios/`,`/cobertura`,`/solicitar`; disallow `/admin`,`/s/`,`/api/`), `sitemap.ts` (`/`, `/solicitar`, `/servicios`, `/cobertura`, 11 `/servicios/[slug]`), `manifest.ts` (theme `#b0522f`), `icon.tsx`, JSON-LD (`Organization`/`WebSite`/`FAQPage` on `/`; `Service`+`BreadcrumbList` on each service page; `BreadcrumbList` on `/servicios` + `/cobertura`). **Service pages** `/servicios/[slug]` from `src/config/service-content.ts` — real, non-duplicated per-trade prose (D10), `generateStaticParams`, `generateMetadata` + canonical. **`/cobertura`** — `COVERAGE` list + `CoverageChecker` client component (no PII, no network) with the D-013 wider-area rule. **CSP (#29)**: `src/proxy.ts` per-request nonce; `force-dynamic` layout. | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build`, `curl` sitemap/robots/manifest/icon on `localhost:3210` (`npm run start`), Claude-in-Chrome drive of a service page, `/cobertura` (checker with PC 46900 → wider-area result), and `/solicitar` (assistant hydrates past the spinner, advances to triage) | Pure logic (`sanitizeProps`, consent gate, `checkCoverage` wider-area) written test-first — `analytics.test.ts` (9), `coverage.test.ts` (+2). `proxy.test.ts` (6) is the failing reproduction for #29 (no CSP from proxy → 5 red), written before the fix. | ✅ 200/200 pass (was 183; +9 analytics, +6 proxy, +2 coverage) ; lint/typecheck/build clean ; curl of all SEO endpoints OK ; JSON-LD blobs valid ; browser: service page + `/cobertura` checker + assistant all hydrate, console clean | 2026-08-30 |

| TP-16 | Sprint 15 / issue #19 (E2E + observability + deploy) | **Observability** `src/lib/observability.ts` — `buildAiMetric` (clamps duration, keeps numbers/enums only), `timeAiCall` (records ok/error/exception outcome, passes the Result through), `reportError` (redacted by the logger). Wired into `analysisService`/`coverageService` (`ai.call` metric) and `POST /api/cron/retention` (try/catch → structured error + 500). `src/app/global-error.tsx` root boundary. **E2E** (`npm run test:e2e`, Playwright, `scripts/e2e-run.mjs` → `docs/.keel/e2e-status.json`): `desktop-chromium` + `mobile-chrome` (Pixel 5). smoke (landing/service page hydrate + no console errors, `/cobertura` checker, robots/sitemap/manifest/JSON-LD), assistant flow 1 (intent→…→contact→"Solicitud recibida", no console errors), flow 2 (sparse text → "needs more info"), authz (`/admin`→login, wrong password rejected, tampered `/s/` token refused, seeded admin signs in), axe a11y × 5 pages. **a11y fix (L-005)**: `--c-text-faint` `#8a8172`→`#6b6353`, `--c-brand` `#c05f3c`→`#b0522f`; landing 23 axe violations → 0. **CI** e2e job added. `docs/{deploy-hostinger,runbook,known-limitations}.md`. | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build`, then `npm run start` (127.0.0.1:3000, dev DB migrated+seeded) + `E2E_NO_SERVER=1 npm run test:e2e` (both projects), `curl /api/health` | Pure logic (`buildAiMetric`, `timeAiCall`) written test-first — `observability.test.ts` (7). No bug-fix bypassed reproduction-first (L-005 caught by the axe pass, fixed, axe pass is now the regression check). | ✅ 207/207 vitest (+7) ; lint/typecheck/build clean ; **30/30 e2e pass on desktop-chromium AND mobile-chrome** ; axe 0 serious/critical on 5 pages × 2 viewports ; `/api/health` 200 ; `docs/.keel/e2e-status.json` result:pass | 2026-08-30 |

| TP-17 | Sprint 16 / issue #22 (verified professional network) | **Domain (test-first)**: `professionals/state-machine.ts` (6 states, reason-to-reject/suspend, RECHAZADO terminal), `professionals/assignment.ts` `checkAssignment` (status/trade/zone/regulated-credential, reports all blocks at once), `professionals/client-view.ts` (`buildClientProfessionalView` — never "verificado" on CONTACT-only, photo only with consent). **Integration**: `professionalService` (create→CANDIDATO + audit log; invalid transition + reason-required; verification who/when/what; private blob + verified delete; SUSPENDIDO ends active assignments but keeps rows; `expiringItems`) + `assignmentService` (blocks incompatible with reasons; regulated trade blocked then allowed once a valid credential is added; substitute keeps both rows; client view not-verified on CONTACT). Migration `20260830213849_professional_network`. Cron runs `purgeRejectedDocuments`. Admin UI (`/admin/profesionales` + `[id]` + `AssignPanel`), client `AssignedProfessional` on `/s/[token]`. Login rate limit 8→20/5min. | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build`, `npm run start` (127.0.0.1:3000, dev DB migrated+seeded) + `E2E_NO_SERVER=1 npm run test:e2e` | Pure logic written test-first — `state-machine.test.ts` (6), `assignment.test.ts` (10, red observed: module-missing). `client-view` + integration tests concurrent with impl (transcription of the issue's explicit rules). No bug fix bypassed reproduction-first. | ✅ 237/237 vitest (+30) ; lint/typecheck/build clean ; **29 E2E pass** (3 mobile admin-panel tests skipped — desktop tool) incl. `professionals.spec.ts` (create → guardrail → APROBADO → verification, console clean) ; `e2e-status.json` result:pass | 2026-08-31 |

| TP-18 | Sprint 17 / issue #21 (Carta de Confianza) | **`src/config/trust-charter.ts`** (test-first, 6 tests): versioned charter, every `commitment` carries a non-empty `backing` (AC "cada promesa → función real"); `version`/`effectiveDate` format; `preAcceptanceCosts` mentions visita + IVA + adicional; `commitmentsAtAcceptance()` is a flagged subset. **`/confianza`** page — full charter, version + effective date, "Gestionado por Praetoria" seal + "no es una certificación" note, Praetoria-provides vs professional-executes split, costs block, "future versions only" note; in `sitemap.ts` + `robots.ts` allow + landing footer. **Landing** trust section renders the charter summary. **`/s/[token]`** "Antes de aceptar" block (costs + `atAcceptance` commitments + charter version) above the decision buttons. **`clientLink.decideQuote`** records `charterVersion` on `Quote.decisionEvidence` (`clientLink.test.ts` asserts `ev.charterVersion === "1.0"`). **a11y**: in-text links underlined (`link-in-text-block`); `/confianza` added to the axe pass. | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build` (standalone), `npm run start` + `E2E_NO_SERVER=1 npm run test:e2e -- --workers=1` | `trust-charter.test.ts` written test-first (module-missing red). No bug fix bypassed reproduction-first. | ✅ 243/243 vitest (+6) ; lint/typecheck/build clean ; **33 E2E pass** (3 mobile admin skipped) incl. `/confianza` smoke + axe (0 serious/critical, both viewports) ; `e2e-status.json` result:pass | 2026-08-31 |

| TP-19 | Sprint 18 / issue #23 (cierre de servicio + incidencias) | **Domain (test-first, 10)**: `service-closure/incidence.ts` — 5-state incidence machine, **RESUELTA/DESESTIMADA require reason AND evidence** (both refusals asserted), SLA `firstResponseDueAt`/`isOverdue`, `WarrantyKind` LEGAL/COMERCIAL/CORTESIA labels. **Integration (8)**: `serviceClosureService` — recordCompletion pulls the accepted quote version; `confirmByClient` needs a completion then moves the request to CERRADA; incidence can't resolve without reason then without evidence then succeeds; `listOpenIncidences` overdue flag; `buildExpediente` carries the economic trace (quote version + approved extras + client confirmation). `reviewService` — refuses a review unless CERRADA; publication needs authorize **and** consent; no-consent review never listed. Migration `20260830223651_service_closure_incidences`. **UI**: `/s/[token]` `PostService` (warranty, confirm[phone last-4], "Tengo un problema", review, expediente download); admin `CompletionPanel` + `/admin/incidencias` (open + due + pending reviews); landing "Opiniones de clientes" (only AUTORIZADA + consent + CERRADA, hidden when empty). | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build` (standalone), `npm run start` + `E2E_NO_SERVER=1 npm run test:e2e -- --workers=1` | `incidence.ts` written test-first (module-missing red). `client-view`/service integration tests concurrent with impl (transcription of the issue's flow + rules). No bug fix bypassed reproduction-first. | ✅ 261/261 vitest (+18) ; lint/typecheck/build clean ; **34 E2E pass** (3 mobile admin skipped) incl. `/admin/incidencias` smoke ; `e2e-status.json` result:pass | 2026-08-31 |

| TP-20 | Sprint 19 / issue #24 (editorial CMS) | **Domain (test-first, 13)**: `content/slug.ts` (accent/space/case normalisation, shape validation), `content/article-status.ts` (5-state machine — **no PUBLICADO/PROGRAMADO without `reviewedByHuman`**, PROGRAMADO needs a future date), `content/blocks.ts` (Zod discriminated union of 9 block types; `headingsOf`/`faqItemsOf`), `content/quality.ts` (`articleWarnings` — dup title/slug, missing author/meta, images without alt). **Integration (8)**: `contentService` — create → BORRADOR + first revision; invalid slug + `slug_taken`; `invalid_body`; publish blocked without human review then allowed; **301 `SlugRedirect` on a published slug change**; scheduled article auto-published by `publishDue`; revision restore; quality warnings. Migration `20260830230038_editorial_cms`. **UI**: `/admin/contenido` + `[id]` editor (block JSON + SEO + warnings + history/restore + preview) + `[id]/preview` (same `BlockRenderer` as public); public `/guias` + `/guias/[slug]` (Article/BreadcrumbList/FAQPage JSON-LD, `generateMetadata`, noindex, `permanentRedirect`); `sitemap.ts` async — appends published articles; cron `publishDue`. **L-006** fixed (deferred-updater `e.currentTarget`). | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build` (standalone), `npm run start` + `E2E_NO_SERVER=1 npm run test:e2e -- --project=<one at a time> --workers=1` (machine OOMs both projects at once) | `content/content.test.ts` written test-first (module-missing red). Service tests concurrent with impl. **L-006 bug caught by the E2E**, fixed, and the E2E extended to the full create→edit→save walk (a reproduction of the crash). | ✅ 282/282 vitest (+21) ; lint/typecheck/build clean ; **37 E2E pass** (5 mobile admin skipped, 0 failed; run per-project) ; `e2e-status.json` result:pass ; editor + preview + `/guias` browser-verified | 2026-08-31 |

| TP-21 | Sprint 20 / issue #25 (local SEO architecture) | **Domain (test-first, 6)**: `local-seo/local-page.ts` `isLocalPageIndexable` — published + not noindex + covered municipality + real coverage note + ≥2 of {typical services, response-time note, local FAQ, completed-jobs note, photo note}; reports every failing reason at once. **Config (9)**: `src/config/problems.ts` (15 curated problems) — unique kebab slugs, every `trade` a known trade, every `relatedProblems` slug resolvable, no self-reference, real content per entry (`problems.test.ts`). Migration `20260831080332_local_seo_pages` (`LocalPage` + `LocalPageStatus`). **Integration (9)**: `localPageService` — create → BORRADOR + slug from municipality; duplicate slug suffixed; unknown trade rejected in `serviceKey`/`typicalServices`; malformed FAQ rejected; unpublished never served publicly; published page `indexable` only with real content; not-covered municipality kept out of `listIndexable`; noindex flag honoured; admin list carries eligibility verdict + reasons. **Public**: `/problemas` + `/problemas/[slug]` (static, `generateStaticParams`, breadcrumb JSON-LD, `TrackedCta`→`landing_cta_click`, service↔problema cross-links); `/zonas` + `/zonas/[municipio]` (DB-backed, `notFound()` when no row, `robots noindex` + no `Service`/`FAQPage` JSON-LD when the guard fails). `sitemap.ts` → `force-dynamic`, adds all problems + only guard-passing zones; `robots.ts` allows `/problemas` + `/zonas`; landing footer indexes both. **Admin**: `/admin/zonas` list (indexable ✓/— + first blocking reason) + `[id]` editor (real-content fields, status buttons, noindex checkbox) + nav link. **a11y**: `.page p a` / `.page dd a` underlined (link-in-text-block); `/problemas`, `/problemas/fuga-de-agua`, `/zonas` added to the axe pass. | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build` (standalone), `npm run start` (127.0.0.1:3000, dev DB) + `curl` HTTP-status/SEO checks + `E2E_NO_SERVER=1 npx playwright test --project=<one at a time> --workers=1` | `local-page.test.ts` + `problems.test.ts` written test-first (module-missing red). Service tests concurrent with impl. One a11y regression (link-in-text-block on the new inline links) caught by the extended axe pass, fixed, axe pass is the regression check. | ✅ 306/306 vitest (+24) ; lint/typecheck/build clean ; HTTP: `/problemas/fuga-de-agua` 200 + `robots index,follow` + canonical ; `/problemas/nope` 404 ; `/zonas/<none>` 404 ; a thin published zone → `robots noindex` + absent from sitemap ; indexable zone → in sitemap + `Service`+`FAQPage` JSON-LD ; **E2E desktop 26 pass / mobile 21 pass, 5 skipped** ; `e2e-status.json` result:pass | 2026-08-31 |

### Guided assistive-technology pass (issue #19 — NOT yet run)
The automated axe pass (TP-16) is the machine half. The screen-reader + full-keyboard walk is a
guided user loop per `references/accessibility.md` and the manual device checklist in
`docs/known-limitations.md` — to be run once against the preview environment before go-live.
Recorded here as `⚠ unverified — ASSISTIVE-TECH` until then.

## Unverified — needs a real environment
- Landing mobile layout was verified from the CSS (single-column grids <40rem, compact hero), not a
  device screenshot — Claude-in-Chrome `resize_window` did not change the screenshot viewport this
  session. Low risk (token-based responsive grid, same system as the rest of the app).
- Real file-picker upload from the `/s/[token]` insurance section is not driven end-to-end in the
  browser (no session-shared file for `file_upload`); the HTTP route was driven with curl and the
  service is integration-tested. Low risk (the client component only does `fetch` with FormData).
- Otherwise none — TP-3, TP-4, TP-9, TP-10, TP-11, TP-12 passed against live PostgreSQL + a real browser.
