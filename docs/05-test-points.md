# 05 — Test points

> One row per test point. `Red first` records the failing-test observation for pure-logic slices (D-007).

| # | Sprint / slice | What was verified | Command(s) | Red first | Result | Date |
|---|---|---|---|---|---|---|
| TP-1 | Sprint 1 / S-003–S-006 pure logic | Request state machine (transitions, actor rules, reason-required, terminals), Money (integer cents, ES parsing, IVA bps, no float), Spanish phone normalisation, signed-link round-trip + tamper + expiry, coverage check | `npm test` | Partial — `money` format assertion written first and observed failing (`'1234,56 €'` vs `/1\.234,56/`); the assertion was over-strict about ICU grouping and was corrected (the requirement — decimal comma + € — was not relaxed). State-machine / phone / signed-link co-written with their tests for the scaffold; bug-fix reproduction-first rule still applies going forward. | ✅ 30/30 pass | 2026-08-29 |
| TP-2 | Sprint 1 / gate | Lint, typecheck, production build | `npm run lint && npm run typecheck && npx next build` | n/a | ✅ all pass; build emits `/`, `/api/health`, `/api/uploads/[...key]` | 2026-08-29 |
| TP-3 | Sprint 1 / DB + runtime | `prisma migrate dev` against live PostgreSQL (docker); `db:seed`; dev server; landing 200; `GET /api/health` → 200 `{status:ok, checks:{database:true}}` | `docker compose up -d && npx prisma migrate dev && npx tsx prisma/seed.ts && npm run dev && curl /api/health` | n/a | ✅ PASS (session 2). Migration `20260829203449_init` applied; seed created 1 admin + 4 requests; `/` returns 200 with the Praetoria landing; `/api/health` returns `database: true`. Fixed L-001 (corrupt migration SQL) and L-002 (blank env var) on the way. | 2026-08-29 |

| TP-4 | Sprint 2 / issue #9 | Draft expiry (pure, test-first), and RequestService integration: draft creation + unique ref + initial event, coverage on describeProblem, contact + 3 granular consents + phone normalisation, valid transition writes attributed StatusEvent, invalid transition rejected with NO event, forbidden actor rejected, idempotent submit, stale-draft deletion | `npx vitest run` (needs docker `praetoria_test` migrated) | S-011 pure helper `isDraftExpired` written test-first (5 assertions); S-013 rejection assertions written before the reject paths were exercised | ✅ 44/44 pass (35 unit + 9 integration) | 2026-08-29 |
| TP-5..TP-8 | Sprints 4–7 | Recorded inline in `docs/sprints/sprint-{4,5,6,7}.md` (photos, assistant flow, admin panel, quotes). | see sprint files | see sprint files | ✅ 88/88 pass at Sprint 7 close | 2026-08-29 |
| TP-9 | Sprint 8 / issue #13 (comms) | Communications domain (templates: brand/name/reference substitution, HTML escaping, QUOTE_AVAILABLE url slot; `canSend` purpose/consent gate — no marketing under operational; `channelForContact`; `idempotencyKey`). `communicationService` integration: idempotent enqueue, `sendPending` delivers + marks SENT, injected send failure → FAILED with request unchanged (AC-13-nolost), `retry` → PENDING → SENT, WhatsApp LINK_PREPARED never auto-sent + deep link carries admin message, unreachable contact refused. Browser drive: admin login → request detail → "Pedir información" → EN_REVISION→REQUIERE_INFORMACION + CommsPanel LINK_PREPARED row + working "Generar enlace de WhatsApp". | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build`, Claude-in-Chrome drive on `localhost:3123` | Pure logic (templates, `canSend`, `channelForContact`, `idempotencyKey`) co-written with 14 assertions — `observed`-equivalent to Sprint 1 precedent (TP-1); no bug-fix here bypassed the reproduction-first rule (L-003 defect caught by the browser drive, then covered by a new integration assertion). | ✅ 108/108 pass (94 unit + 14 comms) ; lint/typecheck/build clean ; browser drive green | 2026-08-30 |

| TP-10 | Sprint 9 / issue #16 (signed client link) | `client-view` domain (every `RequestStatus` → plain label/description, no enum leak; `canDecideQuote`/`canAddInfo`) + `phoneLast4Matches` (formatting-insensitive, wrong/short/missing rejected). `clientLinkService` integration: issue → resolve to own request only; tampered/unknown/id token rejected (no sequential access); revoke stops the link; regenerate needs phone last-4; client view shows comprehensible status + sent quote; accept requires last-4 and records evidence + version; adding requested info re-queues the request. Browser drive of `/s/[token]`: full D4 quote view, `9999` rejected, `5444` accepted → DB `Request ACEPTADA` / `Quote ACEPTADO` / evidence `{via:signed-link,quoteVersion:1,ip}`. | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build`, Claude-in-Chrome drive on `localhost:3126` | Pure logic (`clientStatusView`, `phoneLast4Matches`, `canDecideQuote`) co-written with 13 assertions across 2 files (Sprint 1 precedent). No bug-fix bypassed reproduction-first. | ✅ 121/121 pass ; lint/typecheck/build clean ; browser drive green | 2026-08-30 |

| TP-11 | Sprint 10 / issue #14 (insurance upload) | `insurance` domain (extraction parser: policy no. + ISO validity + coverages/exclusions/franquicias with `{doc,page}` refs; `extractionStatusFor` PENDING/UNREADABLE/PARTIAL/DONE; `missingDocKinds`/`missingSummary`; `detectDocumentType` PDF magic bytes + `validateInsuranceDoc`). `insuranceService` integration: consent gate refuses upload; sensitive blob + `analyze` (OCR + page refs) → PARTIAL + missing summary; verified document deletion; retention `purge`; 1:1 case. Route: 201 upload / 422 bad-type / 403 no-origin. | `npx vitest run` (docker `praetoria_test`), lint, typecheck, `npx next build`, curl + Claude-in-Chrome drive on `localhost:3126` | Pure logic (parser, status, missing, validation) written test-first — 18 assertions (`src/domain/insurance/insurance.test.ts`). No bug-fix bypassed reproduction-first. | ✅ 145/145 pass ; lint/typecheck/build clean ; client + admin browser drive green | 2026-08-30 |

| TP-12 | Sprint 11 / issue #15 (coverage analysis + legal draft, D5) | `coverage` domain (`buildCoverageBreakdown` — D5 three-way split, page refs kept on the clause, legal-norm = process not an invented article; `needsPolicyDocument`; `buildDraft` — HECHOS/PETICIÓN/FUNDAMENTO/ANEXOS, page ref in the fundamento, prudent footer). `coverageService` integration: needs a policy; verdict + breakdown + pending draft; asks for the document when no policy text; `markReviewed` records admin + revision and only then shows "reviewed"; re-analysis resets review; an edit keeps the prior text as a revision. Browser: admin panel D5 split + generated draft + mark-reviewed + revision history; `/s/[token]` client view hides the draft until reviewed then shows it. | `npx vitest run` (docker `praetoria_test`), lint, typecheck, `npx next build`, Claude-in-Chrome drive on `localhost:3126` | Pure logic (breakdown split, needs-document, draft builder) written test-first — 11 assertions (`src/domain/insurance/coverage.test.ts`). No bug-fix bypassed reproduction-first. | ✅ 162/162 pass ; lint/typecheck/build clean ; admin + client browser drive green | 2026-08-30 |

| TP-13 | Sprint 12 / issue #4 (conversion landing) + `/legal/*` | Rebuilt `src/app/page.tsx` (static RSC): hero + 2 CTAs within one screen, cómo funciona, 11 categories, "no necesitas saber", 5 advantages (D3 "un solo interlocutor"), D9 contrast table + recommended message, "por qué puedes confiar" (→#21), data protection, visual quote example (total 159,72 €, "Ejemplo ilustrativo"), insurance block ("podría estar cubierto", never "no pagarás"), coverage from config, realistic FAQ, urgency without 24/7, footer legal links. `/legal/privacidad` + `/legal/aviso-legal` (shared `LegalDoc`, provisional banner, noindex). OG/SEO metadata on `/`. Mandatory-copy rules honoured; zero invented figures. | `npm run lint`, `npm run typecheck`, `npx next build` (`/` + `/legal/*` prerendered static), Claude-in-Chrome drive on `localhost:3130` (`npm run start`) | n/a — static markup (in `references/test-automation.md` not-applied list). | ✅ lint/typecheck/build clean ; 162 tests green ; landing + legal pages render, all CTA hrefs 200 and correct | 2026-08-30 |

| TP-14 | Sprint 13 / issue #17 (security/privacy/retention) | Security headers in `next.config.ts` (CSP/HSTS/nosniff/X-Frame/Referrer/Permissions/COOP, `poweredByHeader:false`) — verified live with `curl -D -`. SSRF guard `src/lib/safe-fetch.ts` (10 tests: v4/v6 private/loopback/link-local/CGNAT/test-net blocks, non-http(s), creds-in-URL, localhost/.internal, redirect re-check). Cross-resource authz + CSRF/origin + PII-redaction (`src/server/security.test.ts`, 7). Retention: `insuranceService.purgeExpired` + `POST /api/cron/retention` (Bearer `CRON_SECRET`, 401 without) + admin export/hard-delete with ops-log (`src/server/services/retention.test.ts`, 5). `docs/threat-model.md` all controls → IN PLACE with evidence; `docs/deploy-hostinger.md`. `npm audit` — remaining high/critical are dev-only. | `npx vitest run`, `npm run lint`, `npm run typecheck`, `npx next build`, `curl -D -` on `localhost:3131` (`npm run start`), `npm audit` | Pure logic (`isBlockedAddress`, `assertSafeUrl`) written test-first — 10 assertions. No bug-fix bypassed reproduction-first. | ✅ 183/183 pass ; lint/typecheck/build clean ; headers + cron-auth verified live | 2026-08-30 |

## Unverified — needs a real environment
- Landing mobile layout was verified from the CSS (single-column grids <40rem, compact hero), not a
  device screenshot — Claude-in-Chrome `resize_window` did not change the screenshot viewport this
  session. Low risk (token-based responsive grid, same system as the rest of the app).
- Real file-picker upload from the `/s/[token]` insurance section is not driven end-to-end in the
  browser (no session-shared file for `file_upload`); the HTTP route was driven with curl and the
  service is integration-tested. Low risk (the client component only does `fetch` with FormData).
- Otherwise none — TP-3, TP-4, TP-9, TP-10, TP-11, TP-12 passed against live PostgreSQL + a real browser.
