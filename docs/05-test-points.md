# 05 — Test points

> One row per test point. `Red first` records the failing-test observation for pure-logic slices (D-007).

| # | Sprint / slice | What was verified | Command(s) | Red first | Result | Date |
|---|---|---|---|---|---|---|
| TP-1 | Sprint 1 / S-003–S-006 pure logic | Request state machine (transitions, actor rules, reason-required, terminals), Money (integer cents, ES parsing, IVA bps, no float), Spanish phone normalisation, signed-link round-trip + tamper + expiry, coverage check | `npm test` | Partial — `money` format assertion written first and observed failing (`'1234,56 €'` vs `/1\.234,56/`); the assertion was over-strict about ICU grouping and was corrected (the requirement — decimal comma + € — was not relaxed). State-machine / phone / signed-link co-written with their tests for the scaffold; bug-fix reproduction-first rule still applies going forward. | ✅ 30/30 pass | 2026-08-29 |
| TP-2 | Sprint 1 / gate | Lint, typecheck, production build | `npm run lint && npm run typecheck && npx next build` | n/a | ✅ all pass; build emits `/`, `/api/health`, `/api/uploads/[...key]` | 2026-08-29 |
| TP-3 | Sprint 1 / DB + runtime | `prisma migrate dev` against live PostgreSQL (docker); `db:seed`; dev server; landing 200; `GET /api/health` → 200 `{status:ok, checks:{database:true}}` | `docker compose up -d && npx prisma migrate dev && npx tsx prisma/seed.ts && npm run dev && curl /api/health` | n/a | ✅ PASS (session 2). Migration `20260829203449_init` applied; seed created 1 admin + 4 requests; `/` returns 200 with the Praetoria landing; `/api/health` returns `database: true`. Fixed L-001 (corrupt migration SQL) and L-002 (blank env var) on the way. | 2026-08-29 |

| TP-4 | Sprint 2 / issue #9 | Draft expiry (pure, test-first), and RequestService integration: draft creation + unique ref + initial event, coverage on describeProblem, contact + 3 granular consents + phone normalisation, valid transition writes attributed StatusEvent, invalid transition rejected with NO event, forbidden actor rejected, idempotent submit, stale-draft deletion | `npx vitest run` (needs docker `praetoria_test` migrated) | S-011 pure helper `isDraftExpired` written test-first (5 assertions); S-013 rejection assertions written before the reject paths were exercised | ✅ 44/44 pass (35 unit + 9 integration) | 2026-08-29 |
| TP-5..TP-8 | Sprints 4–7 | Recorded inline in `docs/sprints/sprint-{4,5,6,7}.md` (photos, assistant flow, admin panel, quotes). | see sprint files | see sprint files | ✅ 88/88 pass at Sprint 7 close | 2026-08-29 |
| TP-9 | Sprint 8 / issue #13 (comms) | Communications domain (templates: brand/name/reference substitution, HTML escaping, QUOTE_AVAILABLE url slot; `canSend` purpose/consent gate — no marketing under operational; `channelForContact`; `idempotencyKey`). `communicationService` integration: idempotent enqueue, `sendPending` delivers + marks SENT, injected send failure → FAILED with request unchanged (AC-13-nolost), `retry` → PENDING → SENT, WhatsApp LINK_PREPARED never auto-sent + deep link carries admin message, unreachable contact refused. Browser drive: admin login → request detail → "Pedir información" → EN_REVISION→REQUIERE_INFORMACION + CommsPanel LINK_PREPARED row + working "Generar enlace de WhatsApp". | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build`, Claude-in-Chrome drive on `localhost:3123` | Pure logic (templates, `canSend`, `channelForContact`, `idempotencyKey`) co-written with 14 assertions — `observed`-equivalent to Sprint 1 precedent (TP-1); no bug-fix here bypassed the reproduction-first rule (L-003 defect caught by the browser drive, then covered by a new integration assertion). | ✅ 108/108 pass (94 unit + 14 comms) ; lint/typecheck/build clean ; browser drive green | 2026-08-30 |

| TP-10 | Sprint 9 / issue #16 (signed client link) | `client-view` domain (every `RequestStatus` → plain label/description, no enum leak; `canDecideQuote`/`canAddInfo`) + `phoneLast4Matches` (formatting-insensitive, wrong/short/missing rejected). `clientLinkService` integration: issue → resolve to own request only; tampered/unknown/id token rejected (no sequential access); revoke stops the link; regenerate needs phone last-4; client view shows comprehensible status + sent quote; accept requires last-4 and records evidence + version; adding requested info re-queues the request. Browser drive of `/s/[token]`: full D4 quote view, `9999` rejected, `5444` accepted → DB `Request ACEPTADA` / `Quote ACEPTADO` / evidence `{via:signed-link,quoteVersion:1,ip}`. | `npx vitest run` (docker `praetoria_test`), `npm run lint`, `npm run typecheck`, `npx next build`, Claude-in-Chrome drive on `localhost:3126` | Pure logic (`clientStatusView`, `phoneLast4Matches`, `canDecideQuote`) co-written with 13 assertions across 2 files (Sprint 1 precedent). No bug-fix bypassed reproduction-first. | ✅ 121/121 pass ; lint/typecheck/build clean ; browser drive green | 2026-08-30 |

## Unverified — needs a real environment
- (none currently — TP-3, TP-4, TP-9, TP-10 passed against live PostgreSQL + a real browser)
