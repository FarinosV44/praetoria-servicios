{
  "//": "Full type-check — app code + tests + tooling configs + prisma/seed. Used by `npm run typecheck` and CI (after `npm run build`, which generates .next/types). `next build` itself uses the narrower tsconfig.json (app only), because the deploy host does a production install without devDependencies.",
  "extends": "./tsconfig.json",
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    "**/*.mts",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts"
  ],
  "exclude": ["node_modules", ".next/standalone", ".next/dev/server"]
}
